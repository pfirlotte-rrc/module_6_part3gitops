apiVersion: apps/v1
kind: Deployment
metadata:
  name: transactions-sync
  labels:
    app: transactions-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transactions-sync
  template:
    metadata:
      labels:
        app: transactions-sync
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: syncer
          image: ghcr.io/pfirlotte-rrc/comp-4001-assignment-6-syncer:fe22d2306a7b2794de9c659f0469fe1938e1142c
          env:
            - name: SRC_URI
              value: mongodb://mongo-backend:27017
            - name: SRC_DB
              value: bank_app
            - name: DEST_URI
              value: mongodb://mongo-transactions:27017
            - name: DEST_DB
              value: transactions_db
            - name: INTERVAL_SECONDS
              value: "300"
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
